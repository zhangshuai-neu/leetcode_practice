class Solution(object):
    def shortestToChar(self, S, C):
        """
        :type S: str
        :type C: str
        :rtype: List[int]
        """
        s_len = len(S)
        ret_list = [ 0 for i in range(s_len)]
        
        c_list = []
        for i in range(s_len):
            
        
        
